# 途龄科技 (Libpet) 机器人工程师 — 面试问题准备

> 岗位：机器人工程师 | 薪资：15-30K | 地点：深圳/香港 | 面试时间：2026.07.04

---

## 一、JD 逐条对应 — 你有的 vs 你没有的

### 你有的（直接匹配）

| JD 要求 | 你的对应经历 | 准备程度 |
|---------|-------------|----------|
| ROS/ROS2 构建完整机器人系统 | Lasso Gripper 项目：上位机通过 ROS 2 节点发出指令实时控制 PWM，STM32 底层执行并反馈传感器数据；MSc 毕设中实际使用 ROS 2 搭建系统 | ⭐⭐⭐ 可直接讲 |
| C++ 和 Python 进行 ROS 开发 | Lasso Gripper 上位机用 Python 写控制节点，底层 STM32 用 C++ 做实时驱动 | ⭐⭐⭐ |
| Linux 开发环境 | 三年半 ARC Lab 都在 Linux 下工作，ROS 2 开发环境全程在 Ubuntu 下 | ⭐⭐⭐ |
| 现场问题定位与排查 | 腱路径摩擦损耗排查（PTFE 导管 + 路径重设计），Lasso Gripper 上电调试中节点通信延迟排查 | ⭐⭐⭐ STAR 故事储备充足 |
| 真实机器人项目开发经验 | Lasso Gripper + 手术器械，两个都是从零搭建的实物原型，其中 Lasso Gripper 完整涉及上位机 ROS → 下位机 STM32 的控制链路 | ⭐⭐⭐ |
| 多传感器集成 | Lasso Gripper：电机编码器 + 电流传感器 + 张力反馈；手术器械：磁编码器 + 总线舵机反馈 | ⭐⭐ |

### 你没有的（需要坦诚 + 学习意愿）

| JD 要求 | 你的状态 | 面试策略 |
|---------|---------|----------|
| LiDAR / Camera 融合调试 | 没用过 LiDAR，Camera 只限于基础了解 | 坦诚说没用过，但你的多传感器集成经验（编码器/IMU/压力传感器）是可迁移的 |
| Nav2 / Autoware 部署调优 | 没用过，只了解基本概念 | 知道它们是做什么的即可，不要装懂。如果被追问说"了解基本架构但没在实际项目中部署过" |
| SLAM 或导航系统开发 | 没做过 | 同上 |
| Docker | 可能用过基础功能 | 如实说"有基本的容器化概念，在实验环境中用过，但没有做过生产级 Docker 部署" |
| Gazebo / Isaac Sim | 没用过 Isaac Sim，Gazebo 在 MSc 期间做过基础仿真搭建 | "我用 Gazebo 做过基础的机器人仿真场景搭建，复杂动力学仿真经验不多" |

---

## 二、必问问题（A 层 — 途龄专用版）

### 1. "简单介绍一下你自己。"（途龄版）

> 我在香港大学 ARC Lab 做了三年半腱驱动机器人研究，独立完成过两套完整的机器人原型系统。一个是 Lasso Gripper，基于 ROS 2 Humble 构建，上位机 Python 节点通过 Topic 通信下发 PWM 指令到 STM32 底层，控制 4 组电机协同调速实现绳索投射—回收的自适应抓取，工作空间比传统夹爪扩大了 157%，发在了 IEEE ROBIO 2025。另一个是手持手术器械的主从遥操作原型，涉及多传感器闭环控制。两个项目都覆盖了从机械设计、3D 打印到嵌入式控制和 ROS 系统集成的全流程。我 MSc 阶段接触 ROS 2 后就很认同它去中心化的设计理念，觉得 DDS 通信框架比 ROS 1 的 Master 模式更适合实际工程部署。我现在住在深圳南山，6 月已经从港大毕业。（约 45 秒）

---

### 2. "你在项目中具体怎么用 ROS 的？用 ROS 1 还是 ROS 2？"

**途龄最可能问的问题。以 Lasso Gripper 为主线回答，体现 ROS 2 经验。**

> 我在 Lasso Gripper 项目里用的是 ROS 2 Humble。上位机跑 Ubuntu 22.04，通过 ROS 2 节点向底层 STM32 发送 PWM 控制指令，调节 4 组电机的协同调速。整个控制链路是：上位机 Python 节点根据抓取策略计算出目标转速 → 发布到 `/motor_cmd` topic → 串口通信节点订阅后通过 USB-UART 下发给 STM32 → STM32 执行底层 PWM 控制并采集编码器反馈 → 反馈数据通过另一个 topic `/motor_feedback` 回传上位机做闭环校验。我在 MSc 毕设阶段接触 ROS 2，觉得它的去中心化架构比 ROS 1 更合理——不需要依赖 Master 节点做注册发现，节点之间通过 DDS 自动发现，Master 挂了整个系统不会一起崩，这对实际工程部署来说是一个很大的改进。

> 手术器械项目则跑在 ROS 1 melodic 上，主要因为当时 ESP32 的 micro-ROS 支持还不够成熟。那个项目里操作端编码器数据通过 `/master_pose` topic 发布，映射节点订阅后计算目标关节角，再通过 `/joint_cmd` 下发给执行端。两个系统加起来大概 6-8 个节点，用 rqt_graph 和 rosbag 做调试和数据分析。

**如果面试官追问 ROS 2 细节：**
- "为什么选 Humble？" → "Humble 是 ROS 2 的第一个长期支持版（LTS），稳定性比 Galactic 好很多，而且配套的 Nav2 和 MoveIt 2 在这个版本上支持最完善。"
- "用什么做节点间通信？" → "ROS 2 默认的 Cyclone DDS。我也了解 Fast DDS 在一些实时性要求高的场景下更合适，但对于我们这个项目的控制频率（50-100Hz），Cyclone DDS 的延迟已经够了。"
- "和 ROS 1 比你最直观的感受是什么？" → "最直观的是不需要先开 roscore。ROS 1 开发的时候习惯性先 roscore 再启动节点，有时候忘记开了就报错。ROS 2 直接用 ros2 run，节点之间自动发现。还有就是 colcon build 比 catkin_make 快，尤其是增量编译的时候。另外 ROS 2 的生命周期节点（lifecycle node）对系统状态管理帮助很大——你可以先配置好节点参数，确认无误后再激活，而不是像 ROS 1 那样启动后就全速跑起来。"

> 我在 Lasso Gripper 项目里用的是 ROS 2 Humble。上位机跑 Ubuntu 22.04，通过 ROS 2 节点向底层 STM32 发送 PWM 控制指令，调节 4 组电机的协同调速。整个控制链路是：上位机 Python 节点根据抓取策略计算出目标转速 → 发布到 `/motor_cmd` topic → 串口通信节点订阅后通过 USB-UART 下发给 STM32 → STM32 执行底层 PWM 控制并采集编码器反馈 → 反馈数据通过另一个 topic `/motor_feedback` 回传上位机做闭环校验。我在 MSc 毕设阶段接触 ROS 2，觉得它的去中心化架构比 ROS 1 更合理——不需要依赖 Master 节点做注册发现，节点之间通过 DDS 自动发现，Master 挂了整个系统不会一起崩，这对实际工程部署来说是一个很大的改进。

> 手术器械项目则跑在 ROS 1 melodic 上，主要因为当时 ESP32 的 micro-ROS 支持还不够成熟。那个项目里操作端编码器数据通过 `/master_pose` topic 发布，映射节点订阅后计算目标关节角，再通过 `/joint_cmd` 下发给执行端。两个系统加起来大概 6-8 个节点，用 rqt_graph 和 rosbag 做调试和数据分析。

**如果面试官追问 ROS 2 细节：**
- "为什么选 Humble？" → "Humble 是 ROS 2 的第一个长期支持版（LTS），稳定性比 Galactic 好很多，而且配套的 Nav2 和 MoveIt 2 在这个版本上支持最完善。"
- "用什么做节点间通信？" → "ROS 2 默认的 Cyclone DDS。我也了解 Fast DDS 在一些实时性要求高的场景下更合适，但对于我们这个项目的控制频率（50-100Hz），Cyclone DDS 的延迟已经够了。"
- "和 ROS 1 比你最直观的感受是什么？" → "最直观的是不需要先开 roscore。ROS 1 开发的时候习惯性先 roscore 再启动节点，有时候忘记开了就报错。ROS 2 直接用 ros2 run，节点之间自动发现。还有就是 colcon build 比 catkin_make 快，尤其是增量编译的时候。另外 ROS 2 的生命周期节点（lifecycle node）对系统状态管理帮助很大——你可以先配置好节点参数，确认无误后再激活，而不是像 ROS 1 那样启动后就全速跑起来。"

---

### 3. "你在 ROS 里具体用了哪些核心机制？怎么理解 TF？"

> TF 我主要用在多坐标系转换上。比如 Lasso Gripper 项目中，抓取目标的位置在相机坐标系（/camera_frame）下，但机械臂的路径规划需要在世界坐标系（/world）或基座坐标系（/base_link）下进行。我用 tf2 库做了静态 TF 广播和动态 TF 监听，通过在 launch 文件中定义 camera → base_link → world 的变换链来实现。实际调试中最常见的问题是 TF 树断链——某个 parent frame 没被广播导致 lookupTransform 失败，用 `tf_echo` 和 `view_frames` 可以快速定位。

---

### 4. "在多传感器融合与调试方面，你有什么经验？"

> 手术器械项目中集成了磁编码器（角度测量）、总线舵机的电流反馈、以及力传感器的数据。三者采样频率不同：编码器 100Hz、舵机反馈 50Hz、力传感器 200Hz。我在 ROS 里用了 message_filters 的 ApproximateTime 同步策略来做时间对齐，然后在融合节点里做了简单的低通滤波处理力传感器的噪声。最大的坑是力传感器和编码器之间的时间戳对齐——两个传感器在不同的硬件线上，系统时间有几十毫秒的偏差。最后我是通过在每个 sensor message 里打上 MCU 本地时间戳，然后在 ROS 节点里统一转换为 ros::Time 来解决的。

---

### 5. "你独立排查过的最难的实机调试问题是什么？"

**用腱路径摩擦损耗的 STAR 故事（途龄版，重点强调独立排查能力和闭环思维）。**

> **Situation**：在 Lasso Gripper 早期原型中，我测到绳索回收时的实际抓取力只有理论设计值的大约 40%，机器人根本抓不住目标物体。

> **Task**：找到力损耗的根本原因并修复，让系统达到可用水平。

> **Action**：我先怀疑驱动电机功率不够——换了更大扭矩的电机，问题没改善。然后测了供电电流，发现电机电流远没到额定值，说明负载端有额外阻力。我拆了整个腱路径，一步一步排查：发现两个问题——一是 3D 打印的导引槽表面粗糙度太高，二是绳索转折角太大，两者叠加导致摩擦力叠加到不可接受的程度。我重新设计了导引路径，用 PTFE 管材做内衬降低摩擦系数，同时减少了两个转折角。改了之后重新打印、重新组装、重新标定。

> **Result**：抓取力恢复到理论值的 85%+，后续实验没有再出现这个问题。这次经历让我养成了"做原型前先考虑制造约束"的习惯——仿真和实物之间的 gap 主要来自制造精度和材料选择，不能只在 CAD 里看起来对就认为实物能跑。

---

### 6. "你对 Autoware / Nav2 了解多少？"

> Autoware 和 Nav2 我都了解一些基本概念。Autoware 是做自动驾驶的完整软件栈，包含定位、感知、规划、控制模块。Nav2 是 ROS 2 的导航框架，核心是基于行为树的导航逻辑和路径规划器体系。坦诚说我没有在实际项目中部署过 Autoware 或 Nav2——我的项目里机器人主要是固定基座的机械臂，没有做自主导航的需求。但我对 ROS 的通信机制和调试工具有比较扎实的使用经验，如果有导航需求，学习曲线对我来说不会太陡。而且我在移动平台的 MSc 课题里做过机械臂和移动底盘的集成，对基础的运动控制有经验。

---

## 三、可能被问的进阶问题（B 层）

### 7. "如果让你基于 ROS 2 从零构建一个机器人系统，你会怎么设计架构？"

> 首先做需求分解——机器人的任务是什么，需要哪些传感器和执行器。然后建节点拓扑图：传感器驱动节点（LiDAR/Camera/IMU）→ 感知/定位节点 → 规划/决策节点 → 控制节点 → 执行器驱动节点。每个节点之间用 Topic 做单向数据流，参数配置和模式切换用 Service。状态机管理我会用 ROS 2 的 lifecycle node 来做，因为 ROS 2 的生命周期管理比 ROS 1 完善得多——你可以在启动时先配置再激活，而不是 ROS 1 那种"开起来就开始跑了"的方式。通信中间件我会选 ROS 2 默认的 Cyclone DDS，配置的话先默认参数跑起来再根据延迟和吞吐量调优。开发过程中用 rosbag2 录数据、rqt 可视化、colcon build 做构建管理。

---

### 8. "ROS 1 和 ROS 2 的核心区别？为什么现在推荐用 ROS 2？"

> ROS 1 基于 XMLRPC 做节点发现，需要先启动 roscore，Master 节点是单点故障——Master 一挂整个系统崩掉。ROS 2 用 DDS 替换了 Master 机制，节点之间通过 DDS Discovery 做去中心化的相互发现。我在 Lasso Gripper 项目里用的是 ROS 2 Humble，最直观的感受就是不需要先 roscore，直接 ros2 run 就行，开发流程顺畅很多。

> 另外 DDS 支持 QoS 配置（可靠性、持久性、截止时间等），对工业级应用更友好。比如你可以在同一个系统里，给安全相关的 topic 配 RELIABLE 模式保证绝不丢包，给高频传感器数据配 BEST_EFFORT 模式降低延迟。ROS 1 做不到这种精细的通信配置。

> ROS 2 原生支持多平台，编译工具从 catkin 换到 colcon，增量编译快不少。生命周期节点（lifecycle node）是另一个我比较欣赏的设计——你可以先配置再激活，而不是启动就运行，这对复杂系统的状态管理特别有用。我个人两个版本都用过，对 ROS 2 的架构理念比较认同，也愿意在新项目中继续深入。

---

### 9. "你对 Docker 和容器化部署了解多少？"

> 我有基础的 Docker 使用概念——知道 Dockerfile、image、container 的关系，用过 docker run 和 docker-compose 在本地起过 ROS 开发环境，主要为了解决 ROS 版本依赖的问题（比如 melodic vs noetic）。但我没有在生产环境做过大规模容器编排或 Kubernetes 级别的部署。如果需要我在岗位上用 Docker 做 CI/CD 或多机器人部署，我会非常乐意深入学。

---

### 10. "如果你加入后要你接手一个已经在运行的机器人系统，你会怎么开始？"

> 第一步先拉代码跑起来——让系统在我本地能编译通过、能运行，这是最快的熟悉方式。然后我对照代码画出实际的节点拓扑（和文档里的设计对比）。第三步跑一遍 rosbag 录的现场数据，看看真实数据流是什么样的，哪些环节有延迟或丢数据。如果有文档就先对着文档理解；没有文档的话我就在跑通后自己写一版系统概述，既是自己学习的输出，也能帮后面的人省时间。最后我会找团队里的老同事过一遍我理解的系统边界，确认没有遗漏关键模块。

---

## 四、途龄业务相关题（了解即可，面试中体现你做过功课）

### 11. "你为什么想来途龄？"

> 我在港大读书的时候就听说 Libpet 入选了福布斯 30 Under 30，加上你们的团队里有香港科大、清华、伯克利背景的人，对技术标准应该比较高。途龄做的是真实场景（机场、场馆、高端出行）的机器人产品，不是 demo 和仿真，这和我自己的经验也很匹配——我做过的两个原型系统都是实物原型，不是纯仿真。另外你们有香港和深圳双办公室，我在港大待了三年半，对深港两地的工作节奏都熟悉，这也是一个天然匹配。

### 12. （如果你有 HK 工作签资格）"你的工作地点偏好？"

> 我现在住在深圳南山，通勤到途龄深圳办比较方便。但如果有需要在香港做项目或者出差我也完全没问题——我在香港住了快四年，两地都很熟悉。

---

## 五、你可以反问的问题

1. "途龄现在产品化的机器人主要部署在哪些场景？传感器配置一般是什么样的？"（体现关心实际产品、同时也帮你了解 LiDAR/Camera 那边的情况）

2. "我加入后最开始会接手什么模块？是维护现有系统还是参与新项目的搭建？"（了解入职后的真实工作节奏）

3. "途龄目前遇到的最头疼的工程问题是什么——比如在实际场地里跑了几天发现某个传感器漂移了或者定位丢了？"（这个问题会让他们觉得你已经有实机调试经验、知道真实问题长什么样）

---

## 六、途龄面试致命底线

- **不要装懂 LiDAR 或 SLAM。** JD 加了这两项但它们是"加分项"而非必须项。你的核心匹配在 ROS 系统集成 + 实机调试 + 全链路工程经验，这几个方面途龄面试官大概率会深度追问。LiDAR/SLAM 如果被问到了坦诚说"了解概念但没实际做过"就行。

- **不要装懂 ROS 2。** 你在 Lasso Gripper 项目里实际用过 ROS 2 Humble，可以把这点讲清楚。不需要说自己用过 ROS 2 Jazzy 或者懂 DDS 源码级别的细节——你讲出"为什么选 Humble（LTS 稳定性）"、"Cyclone DDS 对 50Hz 控制够用"、"colcon vs catkin 的区别"这些真实的开发体验，比背 ROS 2 的新特性列表有说服力得多。

- **清楚地分开讲 Lasso Gripper（ROS 2）和手术器械（ROS 1）。** 让面试官感受到你对两个版本都有实战经验，能从工程角度说出选型理由，而不是背八股。

- **途龄注重"非纯仿真经验"。** 你的两个项目都是实物原型，反复强调"从 SolidWorks 到 3D 打印到上电调试"这条链路，这是你相比纯 ROS 软件工程师的差异化优势。

---

## 七、面试前 15 分钟快速复习

- [ ] 途龄版自我介绍（45 秒版本）
- [ ] 手术器械 ROS 架构（6 个节点、Topic/Service、rosbag）
- [ ] 腱路径摩擦排查 STAR 故事
- [ ] Nav2/Autoware 诚实回答（"了解概念，没实际部署过"）
- [ ] ROS 1 vs ROS 2 的区别（Master → DDS, XMLRPC → Discovery）
- [ ] 反问问题（产品场景/入职模块/最头疼的工程问题）
